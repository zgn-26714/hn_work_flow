1.0 -> 1.1:
1. frames 
调整密度后保持体系分子数的比例不变
manage:
    增加了锁定比例关系和强制更改分子数量的功能
    1.锁定比例关系：在调整密度时，如果设置了FIXED_MOL，那么在调整密度的过程中会严格保证分子数量保持原本的比例；
    注意，随意设置将导致无法达到目标密度！！！因为整数之间的比例关系是量子的而不是连续的
    例如： 在nacl水溶液的模拟中，    sol 5623； na 11； cl 11
           如果强制锁定三者的比例关系，那么下一步三者的比例只能是 11246 ： 22 ： 22 ，因为需要保持5623：11：11的关系不变！
           所以，在设置 FIXED_MOL 之前，请设置良好的初始分子比例，最好能被2整除；或者将溶剂分子排除在外
    2.强制更改分子数量：在调整密度时，有时调整因子为1.1或0.9，而原始分子数量只有20，如果结果采用四舍五入，分子数量将没有变化
           这时，可以通过设置FORCE_MOL参数来强制更改分子数量，分子数将向上取整
           例如： 20*1.1=20.2，四舍五入位20，而向上取整后为21

           注意！！：强制更改分子数量可能会打破原有的比例关系，如果配合 FIXED_MOL ,极易导致意料之外的后果，请谨慎使用

增加完成后从某一步重新开始的功能？
    how:
    1.每一步完成后追加标志文件
    2.读取标志文件，确认从哪一步开始

    manage:
    1.在每一步完成后，调用append_step_marker函数将当前步骤的标志写入.step.cpt文件中，以便下次运行时能够检测到上次完成的步骤。
    2.在脚本开始时，调用detect_resume_step函数读取

能量最小化崩了怎么确认？
    how:
    1.读取gromacs输出或是直接获取终端上的gromacs输出

    manage:
        在check_density.sh中增加对能量最小化失败的检测，
        通过检查mini_pre_eq.log文件中的错误信息来判断是否发生了能量最小化失败，
        并在后续模拟出错时输出相应的错误提示。    


记录每一步系统的电解液分子数量变化
    how:
    1.每一步完成后记录分子数量到日志文件中
    manage:
    在check_density.sh中增加记录每一步系统的电解液分子数量变化，保存在./result/build_model_info.dat中

更换调密度的逻辑，
    现在是不管密度比设定密度更大还是更小，都会从建模开始；
    我想要如果密度比设定密度更大，就直接删去一部分分子，如果密度比设定密度更小，才从头开始

文件逻辑重整
    原始文件如topol.top和packmol.inp在调密度的时候总是要更改，如果某一步出现未知错误可能导致他们描述的分子数量不匹配
    我想在进行调密度前，把topol.top和packmol.inp备份一下，命名成.top和.inp文件，而调密度的时候改的是正常的INPUT中指定的文件

2.run
    取消核数的硬编码

    todo:内置每个集群默认的核数,也可以根据情况调整 (应该已经差不多了，检查一下jiaocha和武超)

3.analysis
    MD_PP不能使用gmx命令

   增加检测,如果使用gmx xxx,进入新的分支


todo:
    1.frames模块会生成很多文件夹，也需要很多文件，看上去有些混乱，需要重新整理一下
    2.frames模块的输出方式，可能有些不太醒目，或者不太清晰，需要重新设计一下输出方式
    3.将以前写过的有用的后处理code集成到MD_PP中
    4.在deal_data模块中增加写过的有用的功能