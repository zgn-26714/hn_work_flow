iter的表现形式和输出样式 （不急）

集成deal_data模块（主要当作备份使用，JE模型的除外）(不急)

slowdir参数的使用(不急)

重整四大模块的输出(不急)

INPUT文件一个集群一个？(不急)


三维surf图
remake_angle
rotate model

todo：
1. frames 
调整密度后保持体系分子数的比例不变
    how：
    1.更换成C++代码，易于维护
    2.确认分子之间的比例，确认是否有明确的整数比
    3.当两种分子之间存在明确的整数比时，记录调整顺序，优先调整少的，而后乘上比例
增加完成后从某一步重新开始的功能？
    how：
    1.每一步完成后追加标志文件
    2.读取标志文件，确认从哪一步开始
能量最小化崩了怎么确认？
    how:
    1.读取gromacs输出或是直接获取终端上的gromacs输出