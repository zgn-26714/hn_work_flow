使用说明：
1.使用前准备：使用前新建一个文件夹，里面分别放置：
1.1 自动建模+获取初始结构模块(NPT下进行体相建模)：
    体相建模和电极建模需要的top文件（包括所有所需的itp文件）
    能量最小化和模拟时所需的mdp文件（NPT模拟时会直接在用给模拟的mdp文件中打开压力耦合项，所以需要事先将压力耦合写好）
    体相建模和电极建模需要的inp文件（用作packmol,packmol中的分子名称和top文件中的要保持一致）
    所有所需的pdb文件
    INPUT文件
1.2 自动提交任务：
    (检查run_md下的job脚本文件是否为自己所需)
    初始结构文件夹 initialframes （里面放置初始结构gro文件和top文件）
    matrixdata文件夹 matrixdata （里面放置matrix数据文件）
    basicfile文件夹 basicfile （里面放置力场、index和grompp文件）
1.3 分析模块
    分析模块是个开放性的框架，使用者可以自行往内部添加分析脚本，详细见  ./analysis/analysis.sh
    在analysis.sh添加自己的分支后，使用./md_flow.sh [analysis] [your_analysis_name] 来调用
    建议维持框架风格，在analysis.sh仅仅调用下一层级的sh文件，在下一层级sh文件中调用具体cpp/python/matlab代码
    使用INPUT文件，将所需参数传递给分析脚本
    notice: 分析模块中完成了一个可以集成所有MDcpp后处理文件的框架 ./analysis/source/MD_PP.sh
            使用者需要将cpp文件的文件名和路径（或是将cpp文件置于./analysis/source/module_C++）写在INPUT文件中
            别忘了指定编译需要的gmx环境
            参数设置见INPUT注释
###########################################################################################################################
# Additional command line arguments for analysis program #
# Classic post-processing commands including -f -s -n -o are already configured. 
# Note that the filenames required for -f and -s should remain consistent and use the DEFFNM parameter for specification.
# Additional custom command parameters need to be entered here.
###########################################################################################################################
2.使用方法：
    修改INPUT中的参数
    bash md_flow.sh 查看详细使用说明
    
3.输出日志：
    生成在 ./result 文件夹中
